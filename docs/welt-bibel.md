# Welt-Bibel — ausgelagert (privat)

Die vollständige **Welt-Bibel** (Canon: Figuren, Setting, Stil) sowie der
persönliche Fragenkatalog, die Art-/Bild-Pipeline-Docs und die
Charakter-Referenzblätter liegen **nicht in diesem öffentlichen Repo**.

Sie befinden sich privat in:

> **`Carlifex/claude-global-skills`** → `gezellig-produktion/`
> (u. a. `gezellig-produktion/docs/welt-bibel.md`)

Grund: `Carlifex/gezellig` ist öffentlich; persönliche und produktionsbezogene
Inhalte sollen nicht öffentlich abrufbar sein — auch nicht in der Git-History
(diese wurde bereinigt). Alles Weitere wird aus der **privaten** Welt-Bibel
erzeugt. Von hier aus nur darauf referenzieren.
